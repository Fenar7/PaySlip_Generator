# Sprint 37.1 Context — 2026-04-25 10:07 +05:30

## Current state

- Branch: `feature/pdf-studio-phase-37-sprint-37-1`
- PR: [#190](https://github.com/Fenar7/PaySlip_Generator/pull/190)
- Commit: `a1139e4abd51b421858199b5b8e1c45fffa7d04d`

## What was fixed

- Unlock upload detection now distinguishes malformed/unsupported PDFs from genuinely password-protected PDFs.
- Protect workspace now uses the shared `encryptPdfViaApi()` client helper instead of hand-rolling `/api/pdf/encrypt`.
- Password length policy is centralized through shared PDF Studio password utilities.
- Encrypt route now rejects malformed owner-password and permission input.
- Unlock upload errors now render immediately instead of only inside the password-entry branch.

## Regression coverage added

- Malformed vs protected unlock detection
- Shared protect encryption path
- Shared max-length boundary behavior
- Malformed permission rejection
- Session storage password stripping

## Verification

- `npx vitest run src/features/docs/pdf-studio/components/protect/protect-unlock-workspace.test.tsx src/features/docs/pdf-studio/utils/password.test.ts src/app/api/pdf/encrypt/__tests__/route.test.ts src/features/docs/pdf-studio/utils/__tests__/storage.test.ts`
- `npx eslint src/features/docs/pdf-studio/components/protect/protect-unlock-workspace.tsx src/features/docs/pdf-studio/components/protect/protect-unlock-workspace.test.tsx src/features/docs/pdf-studio/utils/password.ts src/features/docs/pdf-studio/utils/password.test.ts src/app/api/pdf/encrypt/route.ts src/app/api/pdf/encrypt/__tests__/route.test.ts`
- `npm run test`

## Repo-level blockers outside this sprint

- `npm run build` still fails on an unrelated type error in `src/app/api/books/reports/export/route.ts:244`
- `npm run lint` still fails on pre-existing repo-wide lint debt outside Sprint 37.1

## Notes

- Session password fields remain non-persistent in storage and restore paths.
- Untracked `docs/opencode/` content was left untouched.
