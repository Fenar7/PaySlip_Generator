import FlashReportSettings from "./flash-report-settings";

export const metadata = {
  title: "Flash Report Settings – SW Intel Pro",
};

export default function FlashReportSettingsPage() {
  return <FlashReportSettings />;
}
